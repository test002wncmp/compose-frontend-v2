apiVersion: v1
kind: Service
metadata:
  name: {{ include "compose-fe.fullname" . }}
  labels:
    {{- include "compose-fe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "compose-fe.selectorLabels" . | nindent 4 }}
