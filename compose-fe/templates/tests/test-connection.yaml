apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "compose-fe.fullname" . }}-test-connection"
  labels:
    {{- include "compose-fe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "compose-fe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
